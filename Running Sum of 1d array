class Solution(object):
    def runningSum(self, nums):
        
        running_sum=0
        
        for x in range(len(nums)):
            running_sum += nums[x]
            nums[x] = running_sum
        
        return nums
        
        
